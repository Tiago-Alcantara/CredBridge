@AGENTS.md

# Git
Never run `git commit`, `git push`, or any command that writes to git history. Only generate and edit code. The user handles all commits manually. Never add `Co-Authored-By` lines to commit messages.
